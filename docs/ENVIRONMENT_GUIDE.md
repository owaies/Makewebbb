# Makewebbb environment guide

Keep runtime configuration separate from committed source.

## Local development
Install dependencies with npm, then use the Vite development command from the package manifest.

## Environment variables
Store API keys and other private values in local environment files that are excluded from version control.

## Release checks
Confirm theme switching, route transitions, AI-powered features, and production builds work without exposing private credentials in browser source.
